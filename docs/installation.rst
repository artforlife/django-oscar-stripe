============
Installation
============

At the command line::

    $ easy_install django-oscar-stripe

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-oscar-stripe
    $ pip install django-oscar-stripe
